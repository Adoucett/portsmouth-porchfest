# Copy this file to `.env` and paste your Mapbox PUBLIC token (starts with pk.)
# .env is gitignored and never committed. The real token also lives in Vercel
# project settings (Production + Preview) so the deployed build can read it.
VITE_MAPBOX_TOKEN=pk.your_public_token_here
